<%= form_for(@sttu) do |f| %>
  <% if @sttu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sttu.errors.count, "error") %> prohibited this sttu from being saved:</h2>

      <ul>
      <% @sttu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
